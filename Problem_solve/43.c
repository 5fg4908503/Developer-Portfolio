#include<stdio.h>

int main(){
  int N;
  do
  {
    printf("Enter a positive number: ");
  scanf("%d", &N);
  } while (N <= 0);
  printf("Thanks for entating a pisitive number");
  
  return 0;
}